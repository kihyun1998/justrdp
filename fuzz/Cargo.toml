# Coverage-guided cargo-fuzz / libFuzzer targets for the untrusted-input decoders (ADR-0008,
# issue #99). This is the CI-only counterpart to the in-crate `proptest` no-panic properties:
# libFuzzer's `-timeout` catches hangs (infinite loops) that proptest cannot, and coverage
# guidance reaches deeper paths than random bytes. Requires the nightly toolchain and is
# effectively Linux-only — it never runs on the maintainer's stable Windows MSVC host.
#
# Standalone workspace (the empty `[workspace]` below): cargo-fuzz convention, detaches this
# crate from the parent justrdp workspace so its nightly/libFuzzer build never touches the
# normal `cargo build`.
[package]
name = "justrdp-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = { version = "0.4", features = ["arbitrary-derive"] }
# The decoders under test, by path — the same public entry points the no-panic proptests drive.
justrdp-codecs = { path = "../crates/justrdp-codecs" }
justrdp-pdu = { path = "../crates/justrdp-pdu" }

[workspace]

# Keep some debug info in the fuzz build so crash backtraces are legible.
[profile.release]
debug = 1

# One libFuzzer binary per decoder. `test`/`doc`/`bench` are off — these are fuzz entry points,
# not test or benchmark targets.
[[bin]]
name = "rle"
path = "fuzz_targets/rle.rs"
test = false
doc = false
bench = false

[[bin]]
name = "planar"
path = "fuzz_targets/planar.rs"
test = false
doc = false
bench = false

[[bin]]
name = "clearcodec"
path = "fuzz_targets/clearcodec.rs"
test = false
doc = false
bench = false

[[bin]]
name = "pointer"
path = "fuzz_targets/pointer.rs"
test = false
doc = false
bench = false

[[bin]]
name = "rfx"
path = "fuzz_targets/rfx.rs"
test = false
doc = false
bench = false

[[bin]]
name = "egfx"
path = "fuzz_targets/egfx.rs"
test = false
doc = false
bench = false

[[bin]]
name = "fastpath"
path = "fuzz_targets/fastpath.rs"
test = false
doc = false
bench = false

[[bin]]
name = "capability"
path = "fuzz_targets/capability.rs"
test = false
doc = false
bench = false

[[bin]]
name = "license"
path = "fuzz_targets/license.rs"
test = false
doc = false
bench = false

[[bin]]
name = "nscodec"
path = "fuzz_targets/nscodec.rs"
test = false
doc = false
bench = false

[[bin]]
name = "progressive"
path = "fuzz_targets/progressive.rs"
test = false
doc = false
bench = false

# The connect-sequence and channel-framing parsers (#200). Added once the lane derived its own
# roster, so these are covered from the run they land in rather than whenever someone remembers
# to update a matrix. Undirected bytes already reach 16-49% of each of these files (measured), so
# unlike `progressive` they bootstrap without a seed corpus.
[[bin]]
name = "tpkt"
path = "fuzz_targets/tpkt.rs"
test = false
doc = false
bench = false

[[bin]]
name = "x224"
path = "fuzz_targets/x224.rs"
test = false
doc = false
bench = false

[[bin]]
name = "nego"
path = "fuzz_targets/nego.rs"
test = false
doc = false
bench = false

[[bin]]
name = "dvc"
path = "fuzz_targets/dvc.rs"
test = false
doc = false
bench = false

[[bin]]
name = "svc"
path = "fuzz_targets/svc.rs"
test = false
doc = false
bench = false

[[bin]]
name = "displaycontrol"
path = "fuzz_targets/displaycontrol.rs"
test = false
doc = false
bench = false

[[bin]]
name = "progressive_srl"
path = "fuzz_targets/progressive_srl.rs"
test = false
doc = false
bench = false

[[bin]]
name = "progressive_multipass"
path = "fuzz_targets/progressive_multipass.rs"
test = false
doc = false
bench = false

[[bin]]
name = "progressive_assembly"
path = "fuzz_targets/progressive_assembly.rs"
test = false
doc = false
bench = false

# zgfx (#189): the outermost EGFX decoder. Added with the self-owned decompressor rather than
# after it — the wrapper it replaces had neither a target nor a property, and it is the one
# decoder every EGFX byte crosses.
[[bin]]
name = "zgfx"
path = "fuzz_targets/zgfx.rs"
test = false
doc = false
bench = false
