# MS-RDPEI Implementation Checklist (§9.4 Touch Input)

> **Generated by**: `@spec-checker` (Step 1 of roadmap §9.4)
> **Spec**: [MS-RDPEI] Remote Desktop Protocol: Input Virtual Channel Extension
> **DVC Channel**: `Microsoft::Windows::RDS::Input`
> **Target files**:
> - `crates/justrdp-pdu/src/rdpei.rs` (new)
> - `crates/justrdp-rdpei/src/lib.rs` (new) — or inline in existing DVC host

---

## 0. Crate / Module Layout

- [ ] New file: `crates/justrdp-pdu/src/rdpei.rs` — PDU structs with Encode/Decode
- [ ] New crate: `crates/justrdp-rdpei/` — `RdpeiDvcClient` (`DvcProcessor` impl)
- [ ] `#![forbid(unsafe_code)]` top of every file
- [ ] `#![no_std]` + `extern crate alloc` for `justrdp-pdu` additions
- [ ] DVC channel name: `"Microsoft::Windows::RDS::Input"` (MS-RDPEI §2.1)

---

## 1. Common Header — `RDPINPUT_HEADER` (§2.2.2.6)

| Field        | Type   | Offset | Size    |
|--------------|--------|--------|---------|
| `eventId`    | u16 LE | 0      | 2 bytes |
| `pduLength`  | u32 LE | 2      | 4 bytes |

- [ ] Fixed size: **6 bytes**
- [ ] `pduLength` INCLUDES the 6-byte header
- [ ] `size()` must return header(6) + body
- [ ] Decode: validate `pduLength >= 6`; mismatch → ignore PDU (§3.1.5.1)

### `eventId` Constants (§2.2.2.6)

- [ ] `EVENTID_SC_READY` = `0x0001`
- [ ] `EVENTID_CS_READY` = `0x0002`
- [ ] `EVENTID_TOUCH` = `0x0003`
- [ ] `EVENTID_SUSPEND_INPUT` = `0x0004`
- [ ] `EVENTID_RESUME_INPUT` = `0x0005`
- [ ] `EVENTID_DISMISS_HOVERING_TOUCH_CONTACT` = `0x0006`
- [ ] `EVENTID_PEN` = `0x0008` (future / §9.5; unknown eventIds must be ignored, not rejected)

---

## 2. Variable-Length Integer Encoding

All multi-byte fields are little-endian (§2.2). Variable-length types use a top-bit prefix layout (bit 0 = MSB of first byte).

### 2.1 `TWO_BYTE_UNSIGNED_INTEGER` (§2.2.2.1)

- [ ] Range: `0x0000` – `0x7FFF`
- [ ] First byte layout: `[c(1 bit) | val1(7 bits)]`
  - `c = 0`: 1 byte, value = `val1` (0x00–0x7F)
  - `c = 1`: 2 bytes, value = `(val1 << 8) | val2` (0x0000–0x7FFF)
- [ ] **Boundary**: `0x7F` (1 byte) → `0x80` (2 bytes)
- [ ] Encode: `value <= 0x7F` → `[value as u8]`; else → `[(value >> 8) as u8 | 0x80, value as u8]`
- [ ] Spec example: `0x1A1B` → `[0x9A, 0x1B]`
- [ ] Reject decoded value > `0x7FFF`

### 2.2 `FOUR_BYTE_UNSIGNED_INTEGER` (§2.2.2.3)

- [ ] Range: `0x00000000` – `0x3FFFFFFF`
- [ ] First byte layout: `[c(2 bits) | val1(6 bits)]`
  - `c=0b00`: 1 byte, range 0x00–0x3F
  - `c=0b01`: 2 bytes, range 0x0000–0x3FFF
  - `c=0b10`: 3 bytes, range 0x000000–0x3FFFFF
  - `c=0b11`: 4 bytes, range 0x00000000–0x3FFFFFFF
- [ ] **Boundaries**: 0x3F→0x40, 0x3FFF→0x4000, 0x3FFFFF→0x400000
- [ ] Spec example: `0x001A1B1C` → `[0x9A, 0x1B, 0x1C]` (c=0b10, 3 bytes)
- [ ] Reject > `0x3FFFFFFF`

### 2.3 `EIGHT_BYTE_UNSIGNED_INTEGER` (§2.2.2.5)

- [ ] Range: `0x00` – `0x1FFFFFFFFFFFFFFF`
- [ ] First byte: `[c(3 bits) | val1(5 bits)]`, `c` ∈ 0..7 → 1..8 bytes total
- [ ] **Boundary**: 0x1F→0x20 (1→2 byte transition)
- [ ] Spec example: `0x001A1B1C1D1E1F2A` → `[0xDA, 0x1B, 0x1C, 0x1D, 0x1E, 0x1F, 0x2A]` (7 bytes, c=6)

### 2.4 `TWO_BYTE_SIGNED_INTEGER` (§2.2.2.2)

- [ ] Range: `-0x3FFF` to `+0x3FFF`
- [ ] First byte: `[c(1 bit) | s(1 bit) | val1(6 bits)]`, s=1 negative
- [ ] Spec examples: `-0x1A1B` → `[0xDA, 0x1B]`; `-0x0002` → `[0x42]`

### 2.5 `FOUR_BYTE_SIGNED_INTEGER` (§2.2.2.4)

- [ ] Range: `-0x1FFFFFFF` to `+0x1FFFFFFF`
- [ ] First byte: `[c(2 bits) | s(1 bit) | val1(5 bits)]`
- [ ] Spec examples: `-0x001A1B1C` → `[0xBA, 0x1B, 0x1C]`; `-0x00000002` → `[0x22]`

---

## 3. PDU Wire Formats

### 3.1 `RDPINPUT_SC_READY_PDU` (§2.2.3.1) — Server → Client

| Field              | Type              | Offset | Size | Notes |
|--------------------|-------------------|--------|------|-------|
| `header`           | RDPINPUT_HEADER   | 0      | 6    | eventId = `0x0001` |
| `protocolVersion`  | u32 LE            | 6      | 4    | see constants |
| `supportedFeatures`| u32 LE            | 10     | 4    | **optional**, V300 only |

- [ ] `pduLength` = 10 (V100/V101/V200) or 14 (V300)
- [ ] Determine presence of `supportedFeatures` by **`pduLength == 14`**, NOT by `protocolVersion` alone (§2.2.3.1 uses SHOULD, not MUST)
- [ ] If `pduLength == 10`, treat `supportedFeatures` as absent (0)

### Protocol Version Constants

- [ ] `RDPINPUT_PROTOCOL_V100` = `0x00010000`
- [ ] `RDPINPUT_PROTOCOL_V101` = `0x00010001`
- [ ] `RDPINPUT_PROTOCOL_V200` = `0x00020000` (+ pen)
- [ ] `RDPINPUT_PROTOCOL_V300` = `0x00030000` (+ granular features)

### `supportedFeatures` Flags (SC_READY only)

- [ ] `SC_READY_MULTIPEN_INJECTION_SUPPORTED` = `0x00000001`

---

### 3.2 `RDPINPUT_CS_READY_PDU` (§2.2.3.2) — Client → Server

| Field             | Type            | Offset | Size | Notes |
|-------------------|-----------------|--------|------|-------|
| `header`          | RDPINPUT_HEADER | 0      | 6    | eventId = `0x0002` |
| `flags`           | u32 LE          | 6      | 4    | see below |
| `protocolVersion` | u32 LE          | 10     | 4    | |
| `maxTouchContacts`| u16 LE          | 14     | 2    | |

- [ ] `pduLength` = **16** (fixed)
- [ ] `flags` is **4 bytes** (not 2)

### `flags` Constants

- [ ] `CS_READY_FLAGS_SHOW_TOUCH_VISUALS` = `0x00000001`
- [ ] `CS_READY_FLAGS_DISABLE_TIMESTAMP_INJECTION` = `0x00000002` (SHOULD NOT send to V100 server)
- [ ] `CS_READY_FLAGS_ENABLE_MULTIPEN_INJECTION` = `0x00000004`

### `maxTouchContacts`

- [ ] u16 field (spec has no explicit upper bound)
- [ ] Windows default: 10; roadmap target: 256
- [ ] **Ambiguity**: spec silent on upper limit; implementation policy decides

---

### 3.3 `RDPINPUT_TOUCH_EVENT_PDU` (§2.2.3.3) — Client → Server

| Field         | Type                          | Notes |
|---------------|-------------------------------|-------|
| `header`      | RDPINPUT_HEADER               | eventId = `0x0003` |
| `encodeTime`  | FOUR_BYTE_UNSIGNED_INTEGER    | ms from oldest frame to encode time |
| `frameCount`  | TWO_BYTE_UNSIGNED_INTEGER     | number of frames |
| `frames`      | array of RDPINPUT_TOUCH_FRAME | oldest → newest |

- [ ] `frameCount` range 0..0x7FFF (spec limit is TWO_BYTE max; roadmap 256 = policy)
- [ ] `encodeTime` still valid when `DISABLE_TIMESTAMP_INJECTION` set (server ignores)
- [ ] Total `pduLength` = 6 + sizeof(encodeTime) + sizeof(frameCount) + Σ frame sizes

---

### 3.4 `RDPINPUT_TOUCH_FRAME` (§2.2.3.3.1)

| Field          | Type                          | Notes |
|----------------|-------------------------------|-------|
| `contactCount` | TWO_BYTE_UNSIGNED_INTEGER     | 0..0x7FFF |
| `frameOffset`  | EIGHT_BYTE_UNSIGNED_INTEGER   | microseconds since previous frame; 0 for first |
| `contacts`     | array of RDPINPUT_TOUCH_CONTACT | length = contactCount |

- [ ] First frame in PDU: `frameOffset = 0`
- [ ] Server ignores `frameOffset` when `DISABLE_TIMESTAMP_INJECTION` was set

---

### 3.5 `RDPINPUT_TOUCH_CONTACT` (§2.2.3.3.1.1)

> **Spec name is `RDPINPUT_TOUCH_CONTACT`**, not `RDPINPUT_CONTACT_DATA` as written in the roadmap.

| Field                | Type                         | Notes |
|----------------------|------------------------------|-------|
| `contactId`          | u8                           | fixed 1 byte, 0x00–0xFF |
| `fieldsPresent`      | TWO_BYTE_UNSIGNED_INTEGER    | optional-field presence |
| `x`                  | FOUR_BYTE_SIGNED_INTEGER     | virtual-desktop coords |
| `y`                  | FOUR_BYTE_SIGNED_INTEGER     | |
| `contactFlags`       | FOUR_BYTE_UNSIGNED_INTEGER   | state flags (see combos) |
| `contactRectLeft`    | TWO_BYTE_SIGNED_INTEGER      | optional (CONTACTRECT) |
| `contactRectTop`     | TWO_BYTE_SIGNED_INTEGER      | optional (CONTACTRECT) |
| `contactRectRight`   | TWO_BYTE_SIGNED_INTEGER      | optional (CONTACTRECT) |
| `contactRectBottom`  | TWO_BYTE_SIGNED_INTEGER      | optional (CONTACTRECT) |
| `orientation`        | FOUR_BYTE_UNSIGNED_INTEGER   | optional (ORIENTATION) |
| `pressure`           | FOUR_BYTE_UNSIGNED_INTEGER   | optional (PRESSURE) |

### `fieldsPresent` Flags

- [ ] `TOUCH_CONTACT_CONTACTRECT_PRESENT` = `0x0001` — all 4 rect fields together
- [ ] `TOUCH_CONTACT_ORIENTATION_PRESENT` = `0x0002`
- [ ] `TOUCH_CONTACT_PRESSURE_PRESENT` = `0x0004`

### `contactFlags` Bits

- [ ] `CONTACT_FLAG_DOWN` = `0x0001`
- [ ] `CONTACT_FLAG_UPDATE` = `0x0002`
- [ ] `CONTACT_FLAG_UP` = `0x0004`
- [ ] `CONTACT_FLAG_INRANGE` = `0x0008`
- [ ] `CONTACT_FLAG_INCONTACT` = `0x0010`
- [ ] `CONTACT_FLAG_CANCELED` = `0x0020`

### Valid `contactFlags` Combinations (exactly 8)

| Combination | Hex | Meaning |
|---|---|---|
| `UP` | 0x04 | contact left, out of range |
| `UP \| CANCELED` | 0x24 | canceled while lifting |
| `UPDATE` | 0x02 | position update out of range |
| `UPDATE \| CANCELED` | 0x22 | position update, canceled |
| `DOWN \| INRANGE \| INCONTACT` | 0x19 | new contact on surface |
| `UPDATE \| INRANGE \| INCONTACT` | 0x1A | position update while touching |
| `UP \| INRANGE` | 0x0C | lifted but still hovering |
| `UPDATE \| INRANGE` | 0x0A | position update while hovering |

- [ ] Encoder validates one of these 8 combos
- [ ] Decoder rejects invalid combos

### `orientation` / `pressure` Constraints

- [ ] `orientation` range: 0–359 (`0x000`–`0x167`), 0=y-axis up, increases CCW
- [ ] `pressure` range: 0–1024 (`0x000`–`0x400`)

---

### 3.6 `RDPINPUT_SUSPEND_INPUT_PDU` (§2.2.3.4) — Server → Client

- [ ] `eventId` = `0x0004`, `pduLength` = **6** (header only)
- [ ] Expected bytes: `[0x04, 0x00, 0x06, 0x00, 0x00, 0x00]`

### 3.7 `RDPINPUT_RESUME_INPUT_PDU` (§2.2.3.5) — Server → Client

- [ ] `eventId` = `0x0005`, `pduLength` = **6**
- [ ] Expected bytes: `[0x05, 0x00, 0x06, 0x00, 0x00, 0x00]`

### 3.8 `RDPINPUT_DISMISS_HOVERING_TOUCH_CONTACT_PDU` (§2.2.3.6) — Client → Server

| Field       | Type            | Offset | Size |
|-------------|-----------------|--------|------|
| `header`    | RDPINPUT_HEADER | 0      | 6    |
| `contactId` | u8              | 6      | 1    |

- [ ] `eventId` = `0x0006`, `pduLength` = **7**
- [ ] Only valid for hovering contacts; transitions them to out-of-range

---

## 4. Protocol Version Negotiation

- [ ] Server sends SC_READY with its max version
- [ ] Client responds with CS_READY; negotiated version = `min(server, client)`
- [ ] V300 SC_READY: read `supportedFeatures` iff `pduLength == 14`
- [ ] `DISABLE_TIMESTAMP_INJECTION` SHOULD NOT be sent when server advertised V100
- [ ] **Ambiguity**: client requesting multipen when server didn't advertise it — behavior unspecified

---

## 5. Contact State Machine (§3.1.1.1)

```
Out of Range ──[DOWN|INRANGE|INCONTACT]──> Engaged
Engaged      ──[UP]──────────────────────> Out of Range
Engaged      ──[UP|INRANGE]──────────────> Hovering
Hovering     ──[DOWN|INRANGE|INCONTACT]──> Engaged
Hovering     ──[DISMISS_HOVERING]────────> Out of Range
```

- [ ] Three states: `OutOfRange`, `Hovering`, `Engaged`
- [ ] `UPDATE|INRANGE|INCONTACT` valid only from Engaged
- [ ] `UPDATE|INRANGE` valid only from Hovering
- [ ] Position MUST NOT change on engaged→hovering or engaged→out-of-range transition (§3.1.1.1)

---

## 6. DVC Processor State Machine

```
WaitScReady
  └── on SC_READY (0x0001): store version, supportedFeatures → SendCsReady
SendCsReady
  └── immediately send CS_READY → Ready
Ready
  ├── on SUSPEND_INPUT (0x0004): InputTransmissionSuspended = true
  ├── on RESUME_INPUT (0x0005): InputTransmissionSuspended = false
  ├── API send_touch_frame: if !suspended → send TOUCH_EVENT_PDU
  ├── API dismiss_hovering(id): send DISMISS_HOVERING PDU
  └── unknown eventIds: ignore (§3.1.5.1)
```

- [ ] `InputTransmissionSuspended` ADM (§3.3.1.1): starts `false`
- [ ] When true: MUST NOT send TOUCH_EVENT_PDU
- [ ] Double SUSPEND: ignored (§3.3.5.4)
- [ ] On channel close: reset state

---

## 7. Message Flow

```
Server                                    Client
  │                                          │
  │──── SC_READY (0x0001) ───────────────────▶│  server speaks first
  │                                          │
  │◀─── CS_READY (0x0002) ───────────────────│
  │                                          │
  │◀─── TOUCH_EVENT_PDU (0x0003) ────────────│  0 or more
  │                                          │
  │──── SUSPEND_INPUT (0x0004) ──────────────▶│  server-initiated
  │──── RESUME_INPUT  (0x0005) ──────────────▶│
  │                                          │
  │◀─── DISMISS_HOVERING (0x0006) ───────────│  client-initiated
```

- [ ] Server MUST send SC_READY before client sends CS_READY
- [ ] Client MUST NOT send touch frames before SC_READY
- [ ] Second SC_READY (reconnect) → client re-sends CS_READY

---

## 8. Constraints Summary

| Constraint                   | Value                        | Source        |
|------------------------------|------------------------------|---------------|
| `contactId`                  | 0x00–0xFF                    | §2.2.3.6      |
| `maxTouchContacts`           | u16 (no explicit max)        | §2.2.3.2      |
| `orientation`                | 0–359                        | §2.2.3.3.1.1  |
| `pressure`                   | 0–1024                       | §2.2.3.3.1.1  |
| TWO_BYTE_UNSIGNED max        | 0x7FFF                       | §2.2.2.1      |
| FOUR_BYTE_UNSIGNED max       | 0x3FFFFFFF                   | §2.2.2.3      |
| EIGHT_BYTE_UNSIGNED max      | 0x1FFFFFFFFFFFFFFF           | §2.2.2.5      |
| TWO_BYTE_SIGNED range        | −0x3FFF..+0x3FFF             | §2.2.2.2      |
| FOUR_BYTE_SIGNED range       | −0x1FFFFFFF..+0x1FFFFFFF     | §2.2.2.4      |
| Valid contactFlags combos    | exactly 8                    | §2.2.3.3.1.1  |
| `pduLength` minimum          | 6                            | §2.2.2.6      |

### Spec Ambiguities / Gaps

- [ ] **Max contacts per frame**: no MUST; `contactCount` field is TWO_BYTE max. Roadmap's 256 is policy.
- [ ] **Max frames per PDU**: no MUST; `frameCount` field is TWO_BYTE max. Roadmap's 256 is policy.
- [ ] **Multipen negotiation mismatch**: client requests multipen without server advertisement → unspecified.
- [ ] **V300 `supportedFeatures`**: spec uses SHOULD. Decide presence by `pduLength`, not `protocolVersion`.

---

## 9. Test Vectors (manually derived)

### 9.1 Variable-length integers

- [ ] `TWO_BYTE_UNSIGNED(0x00)` → `[0x00]`
- [ ] `TWO_BYTE_UNSIGNED(0x7F)` → `[0x7F]` (boundary, 1 byte)
- [ ] `TWO_BYTE_UNSIGNED(0x80)` → `[0x80, 0x80]` (boundary, 2 bytes)
- [ ] `TWO_BYTE_UNSIGNED(0x1A1B)` → `[0x9A, 0x1B]` (spec example §2.2.2.1)
- [ ] `TWO_BYTE_UNSIGNED(0x7FFF)` → `[0xFF, 0xFF]` (max)
- [ ] `FOUR_BYTE_UNSIGNED(0x3F)` → `[0x3F]`
- [ ] `FOUR_BYTE_UNSIGNED(0x40)` → `[0x40, 0x40]`
- [ ] `FOUR_BYTE_UNSIGNED(0x001A1B1C)` → `[0x9A, 0x1B, 0x1C]` (spec example §2.2.2.3)
- [ ] `EIGHT_BYTE_UNSIGNED(0x001A1B1C1D1E1F2A)` → `[0xDA, 0x1B, 0x1C, 0x1D, 0x1E, 0x1F, 0x2A]`
- [ ] `TWO_BYTE_SIGNED(-0x1A1B)` → `[0xDA, 0x1B]`
- [ ] `TWO_BYTE_SIGNED(-0x0002)` → `[0x42]`
- [ ] `FOUR_BYTE_SIGNED(-0x001A1B1C)` → `[0xBA, 0x1B, 0x1C]`
- [ ] `FOUR_BYTE_SIGNED(-0x00000002)` → `[0x22]`

### 9.2 Fixed PDUs

- [ ] SC_READY V100: `[0x01,0x00, 0x0A,0x00,0x00,0x00, 0x00,0x00,0x01,0x00]`
- [ ] CS_READY V100 (flags=0x1, maxContacts=10): `[0x02,0x00, 0x10,0x00,0x00,0x00, 0x01,0x00,0x00,0x00, 0x00,0x00,0x01,0x00, 0x0A,0x00]`
- [ ] SUSPEND: `[0x04,0x00, 0x06,0x00,0x00,0x00]`
- [ ] RESUME: `[0x05,0x00, 0x06,0x00,0x00,0x00]`
- [ ] DISMISS_HOVERING (id=0x02): `[0x06,0x00, 0x07,0x00,0x00,0x00, 0x02]`

### 9.3 Roundtrip Tests (encode → decode → assert_eq)

- [ ] SC_READY V100 (pduLength=10)
- [ ] SC_READY V300 + supportedFeatures=0x1 (pduLength=14)
- [ ] CS_READY V101, flags=0x3, maxContacts=255
- [ ] TOUCH_EVENT 1 frame, 1 contact, no optional fields
- [ ] TOUCH_EVENT 1 frame, 1 contact, all optional fields
- [ ] TOUCH_EVENT frameCount=0 (edge case)
- [ ] TOUCH_CONTACT: each of 8 valid contactFlags combos
- [ ] TOUCH_CONTACT: contactId=0 and 255 (bounds)
- [ ] TOUCH_CONTACT: orientation=0, 359; reject 360
- [ ] TOUCH_CONTACT: pressure=0, 1024; reject 1025
- [ ] Variable-int boundary: 0x7F/0x80 (TWO_BYTE), 0x3F/0x40/0x3FFF/0x4000 (FOUR_BYTE)

---

## 10. Reference Files

Existing patterns to mirror:

- `crates/justrdp-rdpsnd/src/processor_dvc.rs` — `DvcProcessor` impl pattern
- `crates/justrdp-dvc/src/lib.rs` — `DvcProcessor` trait, `DvcError`, `DvcMessage`
- `crates/justrdp-pdu/src/lib.rs` — add `pub mod rdpei;`

New files:

- `crates/justrdp-pdu/src/rdpei.rs`
- `crates/justrdp-rdpei/src/lib.rs`

---
---

# Pen Extension (§9.5 — MS-RDPEI V200+ Pen Events)

> **Generated by**: `@spec-checker` Step 1 for roadmap §9.5
> **Append date**: 2026-04-13
> **Spec version**: MS-RDPEI 10.0 (§2.2.3.7, §2.2.3.7.1, §2.2.3.7.1.1, §3.2.5.7, §3.3.1.2, §3.3.5.7)
> **Key insight**: pen rides on the SAME channel (`Microsoft::Windows::RDS::Input`) with `EVENTID_PEN = 0x0008`. Extends existing `justrdp-rdpei` crate — not a new crate.

## 11. Pen PDU Constants

- [ ] `EVENTID_PEN` = `0x0008` (§2.2.2.6; previously noted as future-only, now promoted)

## 12. `RDPINPUT_PEN_EVENT_PDU` (§2.2.3.7)

> **Structural verdict**: IDENTICAL layout to `RDPINPUT_TOUCH_EVENT_PDU`. Differs only in `eventId` and frame/contact element types.

| Field        | Type                          | Notes |
|--------------|-------------------------------|-------|
| `header`     | RDPINPUT_HEADER               | eventId MUST = `0x0008` |
| `encodeTime` | FOUR_BYTE_UNSIGNED_INTEGER    | ms from oldest frame to encode |
| `frameCount` | TWO_BYTE_UNSIGNED_INTEGER     | number of frames |
| `frames`     | array of RDPINPUT_PEN_FRAME   | oldest → newest |

- [ ] `DISABLE_TIMESTAMP_INJECTION` applies to pen by analogy (§2.2.3.2 only names touch fields — **AMBIGUITY**, apply same rule)
- [ ] `InputTransmissionSuspended` also blocks pen events (§3.3.1.1 only names touch — **AMBIGUITY**, treat as universal input gate)

## 13. `RDPINPUT_PEN_FRAME` (§2.2.3.7.1)

> **Structural verdict**: IDENTICAL layout to `RDPINPUT_TOUCH_FRAME`. Differs only in element type.

| Field          | Type                          | Notes |
|----------------|-------------------------------|-------|
| `contactCount` | TWO_BYTE_UNSIGNED_INTEGER     | 0..0x7FFF |
| `frameOffset`  | EIGHT_BYTE_UNSIGNED_INTEGER   | µs since previous frame; 0 for first |
| `contacts`     | array of RDPINPUT_PEN_CONTACT | length = contactCount |

## 14. `RDPINPUT_PEN_CONTACT` (§2.2.3.7.1.1)

| Field           | Type                         | Present     | Notes |
|-----------------|------------------------------|-------------|-------|
| `deviceId`      | u8 (fixed 1 byte)            | always      | MUST be 0 unless multipen negotiated |
| `fieldsPresent` | TWO_BYTE_UNSIGNED_INTEGER    | always      | 5 optional-field flags |
| `x`             | FOUR_BYTE_SIGNED_INTEGER     | always      | |
| `y`             | FOUR_BYTE_SIGNED_INTEGER     | always      | |
| `contactFlags`  | FOUR_BYTE_UNSIGNED_INTEGER   | always      | **same 8 valid combos as touch** (§3.1.1.1 cross-ref) |
| `penFlags`      | FOUR_BYTE_UNSIGNED_INTEGER   | conditional | if `PENFLAGS_PRESENT` |
| `pressure`      | FOUR_BYTE_UNSIGNED_INTEGER   | conditional | if `PRESSURE_PRESENT`, 0..=1024 |
| `rotation`      | **TWO_BYTE_UNSIGNED_INTEGER**| conditional | if `ROTATION_PRESENT`, 0..=359 — **type differs from touch `orientation` (FOUR_BYTE)** |
| `tiltX`         | TWO_BYTE_SIGNED_INTEGER      | conditional | if `TILTX_PRESENT`, −90..=90 |
| `tiltY`         | TWO_BYTE_SIGNED_INTEGER      | conditional | if `TILTY_PRESENT`, −90..=90 |

**Critical differences from TouchContact**:
- ID field semantics (`deviceId` = pen device, not touch point)
- `fieldsPresent` has 5 flags (not 3); no `contactRect`
- `rotation` is 2-byte (touch `orientation` is 4-byte)
- Adds `tiltX`/`tiltY` (signed 2-byte)
- Adds `penFlags` (optional, 4-byte)

### `fieldsPresent` Flags (§2.2.3.7.1.1)

- [ ] `PEN_CONTACT_PENFLAGS_PRESENT` = `0x0001`
- [ ] `PEN_CONTACT_PRESSURE_PRESENT` = `0x0002`
- [ ] `PEN_CONTACT_ROTATION_PRESENT` = `0x0004`
- [ ] `PEN_CONTACT_TILTX_PRESENT` = `0x0008`
- [ ] `PEN_CONTACT_TILTY_PRESENT` = `0x0010`

### `penFlags` Bits (§2.2.3.7.1.1)

- [ ] `PEN_FLAG_BARREL_PRESSED` = `0x0001`
- [ ] `PEN_FLAG_ERASER_PRESSED` = `0x0002`
- [ ] `PEN_FLAG_INVERTED`       = `0x0004`
- [ ] Any OR combination valid (no enumerated valid-combo table, unlike contactFlags)

## 15. Value Range Constraints (§2.2.3.7.1.1)

| Field      | Type                 | Range         |
|------------|----------------------|---------------|
| `pressure` | FOUR_BYTE_UNSIGNED   | 0..=1024      |
| `rotation` | TWO_BYTE_UNSIGNED    | 0..=359       |
| `tiltX`    | TWO_BYTE_SIGNED      | −90..=+90     |
| `tiltY`    | TWO_BYTE_SIGNED      | −90..=+90     |

- [ ] All four ranges validated on encode AND decode (MUST per spec)

## 16. Multipen Negotiation (§2.2.3.1, §2.2.3.2, §3.3.1.2)

Activation requires **all three**:
1. `negotiated_version == V300` (i.e., both server and client_max ≥ V300)
2. Server SC_READY `supportedFeatures & SC_READY_MULTIPEN_INJECTION_SUPPORTED`
3. Client CS_READY `flags & CS_READY_FLAGS_ENABLE_MULTIPEN_INJECTION`

- [ ] When multipen inactive: `deviceId` MUST be `0x00`
- [ ] When multipen active: `deviceId` 0..=3 (4 pens) — **AMBIGUITY**: spec says "up to four" in §2.2.3.1 but doesn't MUST-limit the field. Policy: reject > 3.
- [ ] Client MUST NOT set `ENABLE_MULTIPEN_INJECTION` if server didn't advertise (**AMBIGUITY**: SHOULD NOT, not MUST NOT — enforce in client)
- [ ] Client MUST NOT set the flag when negotiated < V300

### ADM Elements to Track

- [ ] `Pen Input Allowed` (Boolean, §3.3.1.2): set `true` when `negotiated_version >= V200`
- [ ] `Multipen Injection Active` (derived): true iff all 3 conditions above
- [ ] `send_pen_event` MUST check `Pen Input Allowed`

## 17. Pen Contact Count Limits

| Mode                       | Max contacts/frame | Source |
|----------------------------|--------------------|--------|
| V100/V101                  | N/A (no pen)       | §2.2.3.1 |
| V200 single pen            | 1 (policy; spec silent) | §2.2.3.7.1 — **AMBIGUITY** |
| V300 no multipen           | 1                  | same   |
| V300 multipen active       | 4 (one per deviceId 0..=3) | §2.2.3.1 |

- [ ] New policy constant: `MAX_PEN_CONTACTS_PER_FRAME` = 4 (separate from touch's 256)
- [ ] Reuse existing `MAX_FRAMES_PER_EVENT` = 256 for pen PDUs

## 18. DVC Processor Extensions (§3.3.5.7)

Extend the existing `Ready` state:

```
Ready
  ├── [existing touch/suspend/resume/dismiss branches]
  ├── API send_pen_event(encode_time, frames):
  │     if !pen_input_allowed → error (version < V200)
  │     if input_suspended    → error
  │     → enqueue RDPINPUT_PEN_EVENT_PDU
  └── on EVENTID_PEN (0x0008) inbound:
        → ignore (client-sourced, §3.1.5.1)
```

- [ ] Add `EVENTID_PEN` to the inbound-ignore match arm in `process()`
- [ ] Add `pen_input_allowed` field to `RdpeiDvcClient`, set in `handle_sc_ready` based on negotiated version
- [ ] Add `multipen_active` field derived at handshake
- [ ] Add `pen_input_allowed()` / `multipen_active()` accessors

## 19. Pen-Specific Ambiguities

| # | Item | Severity |
|---|------|----------|
| 1 | `DISABLE_TIMESTAMP_INJECTION` coverage of pen fields | SHOULD apply |
| 2 | `InputTransmissionSuspended` coverage of pen events | SHOULD apply |
| 3 | `deviceId` upper bound (0..3 vs field max) | Policy: reject > 3 |
| 4 | Client requesting multipen without server advertisement | SHOULD NOT |
| 5 | V200 pen `contactCount` limit (1 vs field max) | Policy: 1 |
| 6 | `penFlags` valid combinations table | None — all OR combos valid |
| 7 | **CRITICAL**: `rotation` is 2-byte (touch `orientation` is 4-byte) — do NOT reuse the same code path | Critical |

## 20. Test Vectors (Pen)

### 20.1 Minimal PenContact (`deviceId=0, fieldsPresent=0, x=0, y=0, DOWN|INRANGE|INCONTACT`)

```
[0x00, 0x00, 0x00, 0x00, 0x19]  (5 bytes)
```

### 20.2 Minimal PenEventPdu

```
header: [0x08, 0x00, 0x0F, 0x00, 0x00, 0x00]  (eventId=8, pduLength=15)
body:   [0x00]                                  (encodeTime=0)
        [0x01]                                  (frameCount=1)
        [0x01]                                  (contactCount=1)
        [0x00]                                  (frameOffset=0)
        [0x00, 0x00, 0x00, 0x00, 0x19]          (minimal PenContact)
Total:  14 bytes, pduLength=15 (wait: 6+1+1+1+1+5=15 ✓)
```

Full: `[0x08,0x00, 0x0F,0x00,0x00,0x00, 0x00, 0x01, 0x01, 0x00, 0x00,0x00,0x00,0x00,0x19]`

### 20.3 Roundtrip test matrix

- [ ] fieldsPresent = 0 (no optional fields)
- [ ] fieldsPresent = 0x001F (all 5 optional fields)
- [ ] Each of 5 optional fields alone (5 tests)
- [ ] All 10 pairs of optional fields (exhaustive)
- [ ] All 8 valid `contactFlags` combos on PenContact
- [ ] `pressure` boundaries: 0, 1024 accept; 1025 reject
- [ ] `rotation` boundaries: 0, 359 accept; 360 reject
- [ ] `tiltX`/`tiltY` boundaries: −90, 90 accept; ±91 reject
- [ ] `deviceId = 0` accept (non-multipen)
- [ ] `deviceId = 1..=3` in multipen-active mode
- [ ] `deviceId = 4` rejected even in multipen mode (policy cap)

### 20.4 Negotiation tests

- [ ] Server V101 → `send_pen_event` errors (pen not allowed)
- [ ] Server V200 → `pen_input_allowed = true`, `multipen_active = false`
- [ ] Server V300 + MULTIPEN_INJECTION_SUPPORTED, client requests multipen → `multipen_active = true`
- [ ] Server V300 without MULTIPEN_INJECTION_SUPPORTED → `multipen_active = false` even if client requested

## 21. Files to Modify (Pen Extension)

Reuse without change:
- All 5 variable-length integer codecs
- `ContactFlags` constants + 8 valid combinations validation
- `RDPINPUT_PROTOCOL_V*` constants
- `ScReadyFlags::MULTIPEN_INJECTION_SUPPORTED`, `CsReadyFlags::ENABLE_MULTIPEN_INJECTION`
- `MAX_FRAMES_PER_EVENT` DoS cap

Add:
- `crates/justrdp-rdpei/src/pdu.rs`: `EVENTID_PEN`, `PenContact`, `PenFrame`, `PenEventPdu`, `PenFieldsPresent`, `PenFlags`, `MAX_PEN_CONTACTS_PER_FRAME`, pen range constants
- `crates/justrdp-rdpei/src/client.rs`: `send_pen_event` API, `pen_input_allowed`/`multipen_active` fields + accessors, `EVENTID_PEN` to inbound-ignore arm
- `crates/justrdp-rdpei/src/lib.rs`: re-export new pen types/constants
